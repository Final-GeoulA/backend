<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="kr.co.ictedu.geoulA.login.LoginDao">
 <select id="loginCheck" parameterType="uvo" resultType="map">
  select email,password,nickname,1 CNT from users where email=#{email} and password=#{password} group by email,password,nickname
 </select>
 <update id="changePassword">
	  update users set password=#{password} where email=#{email}
  </update>
 </mapper>