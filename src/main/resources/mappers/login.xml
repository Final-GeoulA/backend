<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="kr.co.ictedu.geoulA.login.LoginDao">
 <select id="loginCheck" parameterType="uvo" resultType="map">
  select user_id,user_grade_id,email,password,nickname,age,skin_type,gender,1 CNT from users where email=#{email} and password=#{password} group by user_id,user_grade_id,email,password,nickname,age,skin_type,gender
 </select>
 </mapper>